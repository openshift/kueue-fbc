Add a new catalog directory.

OCP Version: $1

This will create a new directory v$1 and copy the Containerfile.catalog.template into this new directory and give the new name Containerfile.catalog.

This will also update the newly created Containerfile.catalog to use the OCP version specified in $1.

